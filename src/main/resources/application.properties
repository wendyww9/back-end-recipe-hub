spring.application.name=back-end-recipe-hub
## === PostgreSQL DB config ===
#spring.datasource.url=jdbc:postgresql://localhost:5432/recipehub_development
#spring.datasource.username=postgres
#spring.datasource.password=
#
## === JPA config ===
#spring.jpa.hibernate.ddl-auto=update
#spring.jpa.show-sql=true
#spring.jpa.properties.hibernate.format_sql=true

# === Database config (use environment variables) ===
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/recipehubdb}
spring.datasource.username=${DATABASE_USERNAME:postgres}
spring.datasource.password=${DATABASE_PASSWORD:}

# === JPA config ===
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# === Actuator config ===
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=when-authorized